{
  "name": "snipr",
  "version": "2.0.0",
  "private": true,
  "description": "Real-browser sniper console — six live-cam Chromium browsers walking the sniper-site flow (Names → pattern → Randomize → Sniper tab → Start) and reporting the name the site actually sniped",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node test/siteflow.test.js && node test/server.test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "optionalDependencies": {
    "playwright": "^1.49.0"
  }
}
